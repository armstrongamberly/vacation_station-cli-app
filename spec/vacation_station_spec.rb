require "spec_helper"

RSpec.describe VacationStation do
  it "has a version number" do
    expect(VacationStation::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
